{
  "name": "nag-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute nagbot-eu --remote --file=schema.sql",
    "logs": "wrangler tail",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.121.0"
  },
  "dependencies": {
    "chrono-node": "^2.10.1"
  }
}
